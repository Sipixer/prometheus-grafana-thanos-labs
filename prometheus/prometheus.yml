global:
  scrape_interval: 10s
  evaluation_interval: 10s
  external_labels:
    environment: staging

scrape_configs:
  - job_name: "file_sd"
    file_sd_configs:
      - files:
          - /etc/prometheus/sd/*.json
        refresh_interval: 5s
